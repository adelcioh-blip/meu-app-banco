# Copie para .env e preencha para desenvolvimento local
# (O .env está no .gitignore)

# PostgreSQL — pode usar o banco do Render, Supabase, Railway, ou local
DATABASE_URL=postgresql://user:senha@host:5432/radar_licitacoes
