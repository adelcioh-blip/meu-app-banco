{
  "name": "radar-licitacoes",
  "version": "1.0.0",
  "description": "Buscador de licitações PNCP — arrecadação e meios de pagamento",
  "main": "src/api/server.js",
  "scripts": {
    "start": "node src/api/server.js",
    "dev": "nodemon src/api/server.js",
    "build": "npm run build --prefix client",
    "client": "npm run dev --prefix client",
    "validate-query": "node scripts/validate_pncp_query.js",
    "test-filter": "node src/filter/relevance.js",
    "historico": "node scripts/popular_historico.js",
    "diaria": "node scripts/varredura_diaria.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
